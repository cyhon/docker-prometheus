prometheus:
    image: "docker.finogeeks.club/mesons/prometheus"
    ports:
        - "9999:9090"
grafana:
    image: "docker.finogeeks.club/base/grafana"
    ports:
        - "3000:3000"
    environment:
        GF_SECURITY_ADMIN_PASSWORD: abcd@1234
        GF_SERVER_ROOT_URL: http://localhost
    volumes:
        - "/var/monitor/grafana:/var/lib/grafana"
